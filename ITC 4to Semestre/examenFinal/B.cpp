#include <bits/stdc++.h>
using namespace std;

typedef pair<int,int> pi;

int main()
{
	int estaciones, conexiones;
	while (cin >> estaciones >> conexiones)
	{
		if (0 == estaciones && 0 == conexiones) { break; }

		string 
