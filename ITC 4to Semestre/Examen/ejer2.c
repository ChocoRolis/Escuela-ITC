#include <stdio.h>

// PROGRAMA O(n^2 * log(n)) 
// Rolando Rivas 594276
int main()
{
	int n;
	scanf("%d", &n);

	for (int x = 0; x < n; x *= 2)
	{
		for (int j = 0; j < n; j++)
		{
			for (int k = 0; k < n; k++)
			{
				// instrucciones
			}
		}
	}
	
	return 0;
}
// Doy mi palabra que he realizado esta actividad con integridad academica
